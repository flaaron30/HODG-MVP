{
  "name": "hodg-mvp-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "docker compose up --build",
    "lint": "npm -ws run lint --if-present",
    "test": "npm -ws run test --if-present",
    "db:generate": "npm --workspace packages/db run generate"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
